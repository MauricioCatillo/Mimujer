# Base URL para el backend; apunta al dominio donde vive la API.
# Si solo indicas el dominio (p. ej. https://mi-api.com) la aplicación añadirá /api automáticamente.
# Si agregas una ruta (p. ej. https://mi-api.com/api/v1) se respetará tal cual.
# Ajusta el valor antes de compilar con `npm run build`.
VITE_API_URL=http://localhost:4000
