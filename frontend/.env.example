# Base URL para el backend; apunta al dominio donde vive la API.
# Puedes dejar solo el dominio (p. ej. https://mi-api.com) y la aplicación añadirá /api automáticamente.
# Ajusta el valor antes de compilar con `npm run build`.
VITE_API_URL=http://localhost:4000
