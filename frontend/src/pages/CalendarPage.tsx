import RomanticCalendar from "../components/calendar/RomanticCalendar";

const CalendarPage = () => (
  <div className="romantic-card" style={{ padding: 0, background: "transparent", boxShadow: "none" }}>
    <RomanticCalendar />
  </div>
);

export default CalendarPage;
